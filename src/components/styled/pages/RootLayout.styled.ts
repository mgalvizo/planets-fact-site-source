import styled from 'styled-components';
import { RootContent, Component, ComponentContent } from '../Layout.styled';

const StyledRootLayoutContent = styled(RootContent)``;

const StyledRootLayoutComponent = styled(Component)``;

const StyledRootLayoutComponentContent = styled(ComponentContent)``;

export {
    StyledRootLayoutContent,
    StyledRootLayoutComponent,
    StyledRootLayoutComponentContent,
};
